if test -z "$profile_init" ; then
	profile_init=yes
	
	. "$HOME/.profile.init.sh"
	. "$HOME/.profile.colours.sh"
	. "$HOME/.profile.macosx.sh"
	. "$HOME/.profile.prompt.sh"
	. "$HOME/.profile.paths.sh"
	. "$HOME/.profile.banner.sh"
	
fi
